# CHEAT CLIP — Environment Template
# ============================================================
# This app does NOT use a server-side Gemini API key.
# Users provide their own Gemini API key via the web interface at runtime.
#
# Get a free key at: https://aistudio.google.com/
# ============================================================

# Optional: Dedicated proxy ONLY for YouTube transcript fetching (prevents Vercel datacenter IP bans)
# TRANSCRIPT_PROXY=http://username:password@proxy-host:port

# Optional: Custom Invidious instance URL for proxy-free subtitle fetching fallback
# INVIDIOUS_INSTANCE=https://inv.nadeko.net
